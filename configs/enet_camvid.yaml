dataset:
  data_dir: data/camvid
  image_size: [512, 512]
  num_classes: 32
  train_split: 0.8
  augmentations: true

model:
  model_name: enet
  num_classes: 32
  input_channels: 3
  pretrained: false

training:
  batch_size: 8
  lr: 0.0001
  epochs: 50
  num_workers: 4
  seed: 42
  checkpoint_dir: results/checkpoints/enet_camvid
  log_dir: results/logs/enet_camvid
  save_best_only: true
  optimizer_type: Adam
  loss_type: cross_entropy
  weight_dice: 0.5

experiment:
  experiment_name: enet_camvid_baseline
  output_dir: results/experiments/enet_camvid_baseline
